flask
edge-tts
rvc-python==0.1.5

# LUU Y VE fairseq (Phu thuoc cua rvc-python):
# Tren Windows, fairseq duoc cai dat truc tiep tu file wheel dong goi san trong
# thu muc wheels/ (boi scripts/setup.ps1) TRUOC KHI chay requirements.txt nay,
# nham tranh viec pip tai source tu PyPI va yeu cau Visual C++ Build Tools de build.
# KHONG them fairseq vao day de tranh gay xung dot hoac kich hoat build tu source.

# torch / torchaudio KHONG liet ke o day vi ban CUDA can khop voi GPU cua ban.
# Cai theo huong dan rieng trong README.md (muc "Cai dat Python") TRUOC hoac
# SAU khi chay `pip install -r requirements.txt` deu duoc.
